Uses node servers and port 8080

1. Create a codespace
2. Run
   ```
   npm install
   ```
   3. Run
     ```
     npm run build:qemu
     ```
   4. Run
      ```
      npm start
      ```

      Note*:
      You will have to download and load an iso
      
